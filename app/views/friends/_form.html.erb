<%= form_for(@friend, remote: true) do |f| %>
  <% if @friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@friend.errors.count, "error") %> prohibited this friend from being saved:</h2>

      <ul>
      <% @friend.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :age %><br>
    <%= f.number_field :age %>
  </div>
  <%= f.fields_for :shoe do |shoe_form| %>
     <div>
       <%= shoe_form.label :size %>
       <%= shoe_form.text_field :size %>
     </div>
   <% end %>
  
    <%= f.submit "Save", id: 'new_friend_button' %>


<% end %>
